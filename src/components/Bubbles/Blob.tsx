import React from 'react';
import { StyleSheet } from 'react-native';
import type { FunctionalComponent, ReactProps } from '~/types/utils';

interface Props {

}

const Blob: FunctionalComponent<Props>
	= ({ children, style }: ReactProps<Props>) => {
		return (
			<>

			</>
		);
	};

export default Blob;
